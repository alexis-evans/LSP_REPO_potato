Task Manager collaborates with Task because it uses its methods and data; the list of tasks requires the Task class, adding new tasks to the list requires the Task class, and finding or returning tasks also requires the Task class. In essence, TaskManager cannot function without the existence of the Task class. In contrast, however, Task stores, updates, and provides its own data without ever needed external data or methods, making it not a collaborator to TaskManager.